# Shared dependencies for all three members' scripts.
#
# Install a CPU-only torch wheel first if you don't have a GPU (keeps the
# download small):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# On Apple Silicon, the default PyPI torch wheel already includes MPS support,
# so no special index is needed. Then:
#   pip install -r requirements.txt

stable-baselines3[extra]>=2.3.0
gymnasium[atari,accept-rom-license]>=0.29.0
ale-py>=0.9.0
tensorboard
moviepy>=1.0.3
matplotlib>=3.7.0
pygame-ce>=2.5.0
